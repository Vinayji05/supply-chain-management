name: Build Windows simulator

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test echo
        run: echo "Windows workflow syntax OK"
